# Supply Plan Calculator

Веб-приложение для расчета оптимальных планов поставок и оптимизации загрузки контейнеров с 3D визуализацией результатов.

## 📋 Описание

Supply Plan Calculator — это Spring Boot приложение, которое:
- Парсит Excel файлы с данными о товарах и поставках
- Рассчитывает оптимальные планы поставок на основе остатков, продаж и сроков доставки
- Оптимизирует упаковку товаров в контейнеры с использованием алгоритма 3D bin packing
- Генерирует 3D визуализацию упакованных контейнеров
- Экспортирует результаты в Excel файлы

## 🚀 Основные возможности

- **Парсинг Excel файлов**: Загрузка и обработка данных о товарах, поставках и продажах
- **Расчет планов поставок**: Автоматический расчет оптимальных планов на основе остатков и прогнозов продаж
- **Оптимизация упаковки**: Использование алгоритма Largest Area Fit First для оптимальной упаковки контейнеров
- **3D визуализация**: Интерактивный просмотр упакованных контейнеров в браузере
- **WebSocket уведомления**: Отслеживание прогресса расчетов в реальном времени
- **Экспорт результатов**: Скачивание результатов расчетов в формате Excel

## 🛠 Технологический стек

### Backend
- **Java 21**
- **Spring Boot 3.3.4**
- **Spring WebSocket** (STOMP) для real-time уведомлений
- **Apache POI 5.3.0** для работы с Excel файлами
- **3d-bin-container-packing 1.2.17** для оптимизации упаковки контейнеров
- **Thymeleaf** для шаблонов

### Frontend
- **React 17.0.2** для 3D viewer
- **Three.js 0.129.0** для 3D рендеринга
- **Bootstrap 5.3.3** для UI
- **jQuery 3.7.1** для взаимодействия
- **SockJS/STOMP** для WebSocket соединений

## 📦 Требования

- **Java 21** или выше
- **Maven 3.6+**
- **Node.js 18.20.0** (для сборки frontend)
- **npm 10.8.2** (для сборки frontend)

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/ZloyKeks/SupplyPlanCalculator.git
cd SupplyPlanCalculator
```

### 2. Сборка проекта

Проект автоматически соберет frontend при выполнении Maven команды:

```bash
mvn clean package
```

Или для быстрого запуска без пересборки frontend:

```bash
mvn spring-boot:run
```

### 3. Запуск приложения

После сборки запустите приложение:

```bash
mvn spring-boot:run
```

Или используйте собранный JAR:

```bash
java -jar target/SupplyPlanCalculator-0.0.1-SNAPSHOT.jar
```

Приложение будет доступно по адресу: `http://localhost:8080`

### 4. Разработка frontend (опционально)

Для разработки viewer отдельно:

```bash
cd src/main/viewer
npm install
npm start
```

Viewer будет доступен на `http://localhost:3000`

## 📖 Использование

1. **Загрузка файла**: Откройте главную страницу и загрузите Excel файл с данными о товарах
2. **Настройка параметров**: Укажите дополнительные параметры упаковки (доп. размеры, лимит высоты)
3. **Расчет**: Нажмите кнопку расчета и дождитесь завершения (прогресс отображается через WebSocket)
4. **Просмотр результатов**: Скачайте Excel файл с результатами или откройте 3D визуализацию

## 🏗 Архитектура проекта

```
SupplyPlanCalculator/
├── src/main/java/
│   └── by/legan/gt_tss/supplyplancalculator/
│       ├── controllers/          # REST и MVC контроллеры
│       ├── service/              # Бизнес-логика
│       ├── Data/                 # Модели данных
│       ├── visualization/         # Логика визуализации
│       ├── configuration/        # Конфигурация Spring
│       └── webSocket/            # WebSocket DTO
├── src/main/resources/
│   ├── templates/                # Thymeleaf шаблоны
│   └── static/                   # Статические ресурсы
└── src/main/viewer/              # React приложение для 3D viewer
    ├── src/                      # Исходный код React
    └── public/                   # Публичные файлы
```

## 🔌 API Endpoints

### REST API

- `POST /rest/upload` - Загрузка Excel файла
- `GET /rest/calculate/{name}/{user_key}/{dop_l}/{dop_w}/{dop_h}/{max_h}` - Расчет плана поставок
- `GET /rest/calculateFirst/{name}/{user_key}/{dop_l}/{dop_w}/{dop_h}/{max_h}` - Быстрый расчет
- `GET /rest/download/{name}` - Скачивание результата

### WebSocket

- `/websocket` - STOMP endpoint для уведомлений о прогрессе
- `/topic/calc` - Топик для отправки сообщений о прогрессе

## ⚙️ Конфигурация

Основные настройки находятся в `src/main/resources/application.properties`:

```properties
# Порт сервера (по умолчанию 8080)
# server.port=8080

# Настройки загрузки файлов
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Кодировка логирования
logging.charset.console=UTF-8
logging.charset.file=UTF-8
```

## 🧪 Тестирование

Запуск тестов:

```bash
mvn test
```

## 📝 Лицензия

Проект разработан для внутреннего использования.

## 👥 Авторы

- **Legan Andrei** - Разработчик

## 🔄 История изменений

См. [CHANGELOG.md](CHANGELOG.md) для подробной информации об изменениях.

## 🤝 Вклад в проект

1. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
2. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
3. Запушьте в ветку (`git push origin feature/AmazingFeature`)
4. Откройте Pull Request

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории проекта.

---

**Версия**: 0.0.1-SNAPSHOT  
**Последнее обновление**: 2025

